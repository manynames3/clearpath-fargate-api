# Troubleshooting Screenshots

These are public-safe crops from first-run validation troubleshooting. They are kept separate from the main evidence gallery so the README leads with the final healthy deployment state.

| File | Shows |
|---|---|
| `01-ecs-healthcheck-unhealthy-during-validation.png` | Early ECS task health issue during rollout |
| `02-amazon-q-diagnostic-reviewed.png` | Amazon Q diagnostic output reviewed during troubleshooting |
| `03-kms-policy-error-reviewed.png` | KMS console permission warning reviewed during validation |

Do not commit raw troubleshooting screenshots without additional redaction. Raw console output can contain AWS account IDs, full ARNs, security group IDs, endpoint names, and private network identifiers.
